take an input file, count byte frequencies, build a Huffman tree, assign variable-length codes, and then write a compressed bitstream to an output file.

## Features

- Byte-frequency analysis for arbitrary input files
- Huffman tree construction using a priority queue
- Variable-length prefix code generation
- Bit-level output writer for compact encoded files
- Unit tests for core functionality

ECE 264 - sp'22
